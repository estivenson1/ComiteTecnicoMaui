<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ComiteTecnicoMaui.App.Views.CountriesView"
             xmlns:vm="clr-namespace:ComiteTecnicoMaui.App.ViewModels"
             ControlTemplate="{StaticResource TemplateAppGrid}"
             Title="CountriesView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Lista de países" FontSize="Body" FontAttributes="Bold"/>
            <CollectionView ItemsSource="{Binding CountryList}" Margin="5,0">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame CornerRadius="10">
                            <StackLayout>
                                <Label Text="{Binding Name}"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>